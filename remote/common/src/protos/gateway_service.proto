syntax = "proto3";

package terrazzo.gateway;

service GatewayService {
  rpc PingPong(Ping) returns (Pong);
}

message Ping {
  string correlation_id = 1;
}

message Pong {
  string correlation_id = 1;
}

name: CI
on:
  workflow_dispatch:
  push:
    branches:
      - main
  schedule:
    - cron: '0 0 * * 6'
jobs:
  cargo-build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ ubuntu-latest, macos-latest ]
    name: Cargo build and test
    steps:
      - uses: actions/checkout@v6.0.2

      - name: Set up
        uses: ./.github/actions/setup
        with:
          github-token: "${{ secrets.GITHUB_TOKEN }}"
          cache-level: "lenient"

      - name: Build debug
        run: cargo build --locked --bins --features=server,max_level_debug,debug,diagnostics
      - name: Build release
        run: cargo build --locked --bins --features=server,max_level_info --release

      # TODO: how to cache this??
      - uses: actions/setup-node@v6
      - name: Install Playwright
        run: npm ci
      - name: Install Chromium
        run: npx playwright install --with-deps chromium
        
      - name: Run integration test against debug
        run: npm run test:demo:debug
      - name: Run integration test against release
        run: npm run test:demo:release
  bazel-build:
    runs-on: ubuntu-latest
    name: Bazel build and test
    steps:
      - uses: actions/checkout@v6.0.2
      - name: Set up
        uses: ./.github/actions/setup
        with:
          github-token: "${{ secrets.GITHUB_TOKEN }}"
          cache-level: "lenient"
      - name: Bazel build
        run: |
          export PROTOC=$(which protoc)
          bazel build --lockfile_mode=error --action_env=PROTOC //...
      - name: Bazel test
        run: |
          export PROTOC=$(which protoc)
          bazel test --lockfile_mode=error --action_env=PROTOC //...
      - name: Buildifier check
        run: bazel run //bazel:buildifier_check
  publish-dry-run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6.0.2
      - name: Publish dry run
        uses: ./.github/actions/setup
        with:
          github-token: "${{ secrets.GITHUB_TOKEN }}"
          cache-level: "off"
      - uses: ./.github/actions/publish-dry-run

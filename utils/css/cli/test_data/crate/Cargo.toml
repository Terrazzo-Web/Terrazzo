[package]
name = "terrazzo-cli-test-crate"
description = "A dummy crate to test Terrazzo SCSS CLI."

[lib]

[dependencies]
dashmap = { workspace = true }
futures = { workspace = true }

[features]
client = []
default = ["client", "server"]
server = []

[package.metadata.css]
output_file = "target/css/terrazzo-terminal.scss"
folders = ["src/"]
extensions = [".scss", ".css"]

[lints]
workspace = true

name: Build
runs:
  using: "composite"
  steps:
    - name: Compile debug
      run: cargo build --bin web-terminal --features server,max_level_debug
      shell: bash
    - name: Compile release
      run: cargo build --bin web-terminal --features server,max_level_info --release
      shell: bash

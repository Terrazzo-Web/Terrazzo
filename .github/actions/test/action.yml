name: Test
runs:
  using: "composite"
  steps:
    - run: cargo test
      shell: bash
    - run: cargo test --features=client
      shell: bash
    - run: cargo test --features=server
      shell: bash
